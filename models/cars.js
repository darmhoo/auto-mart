const helpers = require('../helpers/helper');
const data = require('../data/cars.json');
const

function Cars(id, owner, createdOn, state, status, price, manufacturer, model, bodType, imageUrls) {


}